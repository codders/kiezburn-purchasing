{
  "rules": {
    "admin": {
      "areas": {
        ".read": true
      }
    },
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
